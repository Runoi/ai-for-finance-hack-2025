class Config:
    """
    Центральный конфигурационный файл для всего проекта.
    Содержит "тумблеры" для управления архитектурой и выбором моделей.
    """
    
    # === НАСТРОЙКИ МОДЕЛЕЙ ===
    # Выбор моделей для разных ролей в агентской системе.
    # Начнем с самых мощных для агентов и сбалансированной для генерации.
    # Модель для эмбеддингов (дешевая и эффективная)
    EMBEDDING_MODEL = "text-embedding-3-small"
    
    # Модель для финальной генерации ответа (баланс цена/качество)
    GENERATOR_MODEL = "openrouter/google/gemma-3-27b-it"
    
    # Модель для "думающих" агентов (максимальное качество логики)
    ANALYST_MODEL   = "openrouter/meta-llama/llama-3-70b-instruct"
    REFINER_MODEL   = "openrouter/meta-llama/llama-3-70b-instruct"

    # === НАСТРОЙКИ АРХИТЕКТУРЫ ===
    # "Тумблеры" для включения/отключения компонентов.
    # Для MVP (Спринт 1) мы отключаем все сложное.
    
    # Включать/выключать "Голову D" (Граф Концептов) в ретривере.
    ENABLE_GRAPH_CONCEPT_HEAD = False
    
    # === НАСТРОЙКИ АГЕНТСКОГО ЦИКЛА ===
    
    # Максимальное количество итераций для FAIR-RAG цикла.
    # Для MVP ставим 1, что эквивалентно простому RAG.
    MAX_ITERATIONS = 1

# Глобальный экземпляр конфига, который будет импортироваться во всем проекте.
config = Config()